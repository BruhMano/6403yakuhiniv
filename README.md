# Dog Image Processor 🐾
**Dog Image Processor** — это высокопроизводительное консольное приложение на Python для асинхронной загрузки и многопоточной обработки изображений собак из Dog API. Проект демонстрирует применение продвинутых технологий: от низкоуровневых алгоритмов компьютерного зрения до асинхронной архитектуры и дистрибуции пакетов.

# 🌟 Основные возможности
**Асинхронная работа:** Загрузка и сохранение файлов через aiohttp и aiofiles без блокировки основного потока.

**Параллельные вычисления:** Использование ProcessPoolExecutor для обхода GIL и ускорения обработки изображений на многоядерных процессорах.

**Собственные алгоритмы (Numba JIT):** Реализация свертки, детекторов Кэнни, Харриса и преобразования Хафа «с нуля» с оптимизацией скорости до уровня C.

**Сравнение с OpenCV:** Возможность верификации ручных алгоритмов через эталонную реализацию OpenCV.

**ООП Дизайн:** Использование абстрактных интерфейсов, инкапсуляции и перегрузки операторов для работы с изображениями как с объектами.

# 🛠 Технологический стек
**Язык:** Python 3.11+

**Сетевое взаимодействие:** asyncio, aiohttp

**Математика и обработка:** NumPy, Numba, OpenCV

**Инструментарий:** Pipenv, build (Setuptools), unittest, flake8

# 📂 Структура проекта
```
├── dist/                # Собранные пакеты (.whl, .tar.gz)
├── implementation/      # Основная логика приложения
│   ├── dog_image.py     # Модели данных и перегрузка операторов
│   ├── image_processing.py # Ручные алгоритмы (Numba JIT)
│   └── dog_image_processing.py # Асинхронный оркестратор
├── interfaces/          # Абстрактные интерфейсы (ABC)
├── logs/                # Журналы работы приложения
├── tests/               # Модульные и асинхронные тесты
├── main.py              # Точка входа в приложение
└── pyproject.toml       # Конфигурация сборки пакета
```
# 🚀 Быстрый старт
## Установка
1. Клонируйте репозиторий:
```Bash
git clone https://github.com/username/dog-image-processor.git
cd dog-image-processor
```
2. Настройте окружение и зависимости:
```Bash
pipenv install
pipenv shell
```
3. Создайте файл .env в корневой папке и добавьте ваш API ключ:
```env
API_KEY=your_secret_key_from_thedogapi
```
## Запуск
Используйте консольный интерфейс для управления процессом:

```Bash
# Скачать 5 изображений, перевести в ч/б и обработать
python main.py --limit 5 --output_dir "results/" --grey
```
## Тестирование
Запуск всех тестов (включая асинхронные и тесты моделей):

```Bash
python run_tests.py
```
# 📦 Сборка и дистрибуция
Для сборки пакета выполните:

```Bash
python -m build
```
После этого вы сможете установить пакет локально:

```Bash
pip install dist/dog_image_processor-1.0.0-py3-none-any.whl
```
И имортировать пакет в свой код или запустить с помощью: `python -m dog_image_processor -l 10 -odir 'res'`.

Можно запустить скомпилированную программу `dog-processor -l 5 -odir 'res'`.

# 📝 Логирование
Приложение ведет подробный журнал в `logs/app.log`:

**DEBUG:** Подробная информация о PID процессов, времени выполнения и стадиях асинхронных задач.

**INFO:** Краткие уведомления о старте, прогрессе и успешном завершении операций в консоли.

---
Разработано в рамках курса "Технологии программирования на Python". Автор: Якухин Иван.
