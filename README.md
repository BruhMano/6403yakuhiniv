# Отчет по циклу лабораторных работ
## Разработка системы Dog Image Processor 🐾

### 1. Цель работы
Программный комплекс «Dog Image Processor» представляет собой инженерное решение, объединяющее низкоуровневую математическую обработку сигналов с современной высоконагруженной архитектурой на базе асинхронных технологий и параллельных вычислений.

Основной целью является создание многофункционального приложения для потоковой обработки изображений и анализа данных с интеграцией внешних API, использованием асинхронных технологий и обеспечением промышленного стандарта качества кода. В ходе выполнения работ осваиваются методы реализации алгоритмов свертки и детекции объектов «с нуля», принципы асинхронного ввода-вывода (aiohttp, aiofiles), многопроцессорные вычисления для обхода GIL, а также стандарты DevOps: логирование, Unit-тестирование и упаковка пакетов в TestPyPI.

---

### 2. Архитектура проекта 🏗️
Архитектура приложения организована иерархически и разделена на несколько функциональных уровней: уровень абстракции, уровень предметной области, уровень реализации алгоритмов и уровень управления.

В основе системы лежит строгое определение контрактов в модуле интерфейсов, где абстрактный базовый класс IImageProcessing задает единый стандарт для методов обработки. Это позволяет реализовать паттерн «Стратегия», обеспечивая прозрачное переключение между движками обработки. Уровень предметной области инкапсулирует данные в иерархии классов DogImage, ColorDogImage и GrayscaleDogImage, поддерживающих арифметические операции над изображениями через перегрузку операторов.



Вычислительная логика разделена на «ручную» реализацию в модуле ImageProcessing, оптимизированную с помощью Numba для достижения нативной скорости, и адаптер для OpenCV. Координацию системы выполняет класс-оркестратор DogImageProcessor, который управляет асинхронным сетевым слоем и распределяет вычислительные задачи по процессам. Инфраструктурный слой включает централизованное логирование, модульное тестирование и конфигурацию окружения, что обеспечивает целостность и масштабируемость всего решения.

---

### 3. Внутренняя реализация ⚙️
Технологический стек проекта выбран для совмещения высокоуровневого управления сетевыми запросами с низкоуровневой оптимизацией вычислений.

Сетевое взаимодействие и файловые операции реализованы на базе неблокирующего ввода-вывода с использованием библиотек asyncio и aiohttp. Это позволяет инициировать множество запросов к Dog API одновременно, не блокируя основной поток выполнения. Для вычислительно сложных задач, таких как свертка или преобразование Хафа, применяется многопроцессорность через ProcessPoolExecutor. Это позволяет задействовать все ядра процессора и обойти ограничения Global Interpreter Lock (GIL) в Python.



Для обеспечения производительности «ручных» алгоритмов используется JIT-компиляция через библиотеку Numba, которая преобразует циклы обработки пикселей в машинный код во время выполнения. Математическим фундаментом служит NumPy, обеспечивающий эффективную работу с матрицами. Архитектура также опирается на продвинутые механизмы ООП, включая перегрузку магических методов для интуитивной работы с объектами. Надежность системы гарантируется ротируемым логированием и автоматизированными тестами, проверяющими как математическую точность алгоритмов, так и корректность асинхронного поведения.

---

### 4. Сборка, дистрибуция и управление пакетом 📦
Завершающим этапом реализации является приведение проекта к формату готового программного продукта через стандарт упаковки PEP 621.

В проекте используется файл pyproject.toml для определения метаданных, зависимостей и консольных точек входа. В конфигурации зарегистрирован скрипт dog-processor, который позволяет запускать приложение из терминала как системную утилиту. Процесс сборки осуществляется модулем build, создающим дистрибутивы в форматах sdist и wheel. Установка выполняется стандартным менеджером pip, который автоматически разрешает зависимости. Для разработчиков предусмотрено управление средой через Pipenv, а готовность к эксплуатации подтверждается успешной публикацией пакета в репозитории TestPyPI с помощью twine.



---

### 5. Инструкция по установке и запуску 🚀

**Установка:**
1. Установить зависимости: `pipenv install`
2. Активировать среду: `pipenv shell`
3. Настроить API-ключ в файле `.env`: `API_KEY=your_key`

**Запуск:**
Приложение запускается командой:
`python main.py -l 5 -odir "results/" -grey`



---

### 6. Заключение 🏁
В ходе выполнения цикла лабораторных работ было спроектировано и успешно реализовано инженерное решение для автоматизированного анализа медиаданных, полностью отвечающее всем методическим требованиям. Приложение подтвердило свою эффективность за счет интеграции современных методов асинхронного программирования и параллельных вычислений, обеспечивающих высокую скорость обработки при минимальных задержках. 

В рамках реализации функциональности было создано консольное решение, обеспечивающее полный цикл обработки: от взаимодействия с внешними REST API до проведения сложных математических манипуляций с изображениями «с нуля». Проект демонстрирует применение профессиональных практик тестирования, логирования и дистрибуции программных продуктов. Итоговый проект подтверждает готовность продукта к эксплуатации в реальных условиях и служит свидетельством глубокого освоения технологий разработки высокопроизводительных систем на языке Python.
